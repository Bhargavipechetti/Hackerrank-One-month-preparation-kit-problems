/**Grading Students**/

def gradingStudents(grades):
    for i in range(len(grades)):
        if(grades[i]>37):
            if((grades[i]%5)==3):
                grades[i]+=2
            elif((grades[i]%5)==4):
                grades[i]+=1
    return (grades)
